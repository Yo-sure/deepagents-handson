---
layout: page
title: 전체 구조를 연결하고 적용점을 정리한다
sidebar: false
aside: false
pageClass: lec-page
---

<div class="lec workshop-edition"><div class="deck">
<section class="slide">
<div class="eyebrow">2026.09 · 개인 실습 · 30분 · 관찰 5 / 독립 변경 15 / 풀이 10</div>

# 전체 구조를 연결하고 적용점을 정리한다

주 실습의 최종 결과는 [내 업무 Agent 완성 기준](./build#finish)으로 확인합니다. `build_lab.runner complete`에서 학생 도구·Agent·분기·MCP 등록·수용 판단과 제공 그래프·수정 루프가 연결된 실행 기록을 설명합니다.

<p class="lead">한 번의 실행을 여러 계층으로 나누어 설명합니다. 새 조건이 생겼을 때 어느 부분을 수정할지 판단합니다.</p>

처음에는 “이 답변이 어디서 나왔는가”를 실행 기록으로 확인했습니다. 이제는 자신이 작성한 코드에서 그 근거가 어떻게 전달되는지 설명할 차례입니다. 마지막 별칭 변경은 새 기능을 많이 붙이는 활동이 아니라, 요청의 표현이 바뀌어도 같은 정책과 검토 계약을 유지할 수 있는지 확인하는 활동입니다.

<div class="cue"><div class="cue-body">모든 명령은 <code>workshop</code> 폴더에서 실행합니다. 처음이라면 <a href="./start">시작 안내</a>를 먼저 확인합니다. 전체 연결에 필요한 앞 단계가 미완료라면 시작 안내의 복귀 절차로 해당 함수만 보완하고 제공받은 부분을 기록합니다.</div></div>
</section>

<section class="slide" id="icebreaker">

## 시작 질문 · 테스트는 초록색인데 사용자는 왜 실패했다고 할까요?

Anthropic의 평가 글은 코드 검사·모델 평가·사람의 판단을 구분하며, 정해진 형태의 검사만으로는 의미와 품질을 놓칠 수 있다고 설명합니다. [2026-01-09 · 원문](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)

**답변에 정확한 팀명과 정책 ID가 있습니다. 이것만으로 올바른 답변이라고 할 수 있을까요?**

새 별칭 요구를 직접 반영하고 기존 동작을 다시 확인합니다. 검사 통과와 실제 답변의 의미를 함께 봅니다.

</section>

<nav class="lesson-nav" aria-label="학습 단계"><a href="#observe">01 통합 실행</a><a href="#practice">02 독립 변경</a><a href="#solution">03 풀이</a></nav>

<section class="slide" id="observe">

## 전체 부품을 관찰합니다 · 5분

이번에는 자신의 구현을 연결한 요청 하나를 추적합니다. `course.integration`은 제공된 완성 예제의 비교 자료이며 학생 파일을 실행하지 않습니다. 최종 확인에는 아래 학생 실행 명령을 사용합니다.

```bash
uv run python -m build_lab.runner complete --topic 정산
```

학생 MCP 서버의 정책 조회→제공 Graph와 학생 분기→학생 LangChain Agent의 초안→제공 루프의 검토·필요 시 수정→A2A 검토→학생 수용 판단을 확인합니다. 초안이 처음부터 통과하면 수정하지 않습니다. 수정이 필요하면 정책·초안·피드백을 모델에 전달합니다.

로컬 검토가 passed인 경우에만 A2A 검토로 넘어갑니다. ask·held·stalled이면 원격 검토를 실행하지 않습니다. 원격 검토가 완료돼도 요청·버전·판정이 맞지 않으면 학생 수용 함수가 보류해야 합니다.

현재 예제는 정책 갱신 배포를 자동 동기화하지 않습니다. DeepAgents는 앞의 별도 예제에서 비교하며, 통합에 프레임워크를 중복으로 얹지 않습니다.

실행 후 명령 마지막에 표시된 `runs/build-complete-고유값.json`에서 아래 근거를 찾아 개인 기록에 옮깁니다. 실행마다 다른 파일이 생성됩니다. 한 파일이 서로 다른 계층의 결과를 담고 있습니다.

| 단계 | 찾을 증거 | 확인할 질문 |
|---|---|---|
|MCP 조회 결과|state.data|조회한 규정 ID와 담당 팀은 무엇인가?|
|LangGraph|state.visited, state.decision|초안 작성 경로를 택한 이유는 무엇인가?|
|LangChain|trace|도구 결과와 답변의 근거가 같은가?|
|수정 루프|state.history, state.draft, state.decision|수정을 했는가? 하지 않았다면 왜인가?|
|A2A 검토|review.state, review.artifact, review.decision|현재 요청의 완료 상태와 검토 통과를 함께 확인했는가?|

조회한 정책은 요청 단위의 snapshot으로 초안 생성에 전달됩니다. Agent의 도구 호출은 이 snapshot을 읽으며 호출할 때마다 MCP 서버를 다시 조회하지 않습니다. `state.data`와 최종 `state.draft`를 직접 대조합니다. HTTP 도구 목록과 응답 포장까지 다시 확인하려면 `build_lab.runner mcp --topic 정산`의 `transport`를 읽습니다. complete 로그에는 그 포장 대신 조회된 업무 데이터가 남습니다.

모델 호출이 실패하면 해당 단계의 오류를 해결한 뒤 다시 실행합니다. 실제 호출과 결과 전달이 확인되어야 통합 실습을 완료한 것입니다.

</section>

<section class="slide" id="practice">

## 독립 변경 · 15분: 업무 별칭을 추가합니다

기존 다섯 구현을 마친 뒤 새 요구를 받았습니다. **로그인 문의는 계정 정책으로, 비용 문의는 정산 정책으로 조회**되어야 합니다. 정책 두 건을 복제하거나 모델 프롬프트에 정답 팀을 추가하지 않습니다. 기존 함수 인자와 반환 구조는 유지합니다.

수정 위치와 구현 방법은 먼저 스스로 정합니다. 다음 계약만 지킵니다.

|입력|반환 topic|결과|
|---|---|---|
|로그인|계정|P-02|
|앞뒤 공백이 있는 비용|정산|P-01|
|정산·계정|기존과 같음|기존 정책 유지|
|로그인 장애·비용 취소|공백만 정리한 원문|미등록, found=false|

별칭은 명시된 두 단어만 지원합니다. 문장에 단어가 들어 있다는 이유로 정책을 추측하지 않습니다. 정책 dict 정본을 바꾸지 않으며, 호출 순서에 따라 결과가 달라져서도 안 됩니다.

**예측·실패 확인 3분 → 구현 7분 → 검사·실제 연결 5분**으로 진행합니다. 시작 전에 현재 구현이 실패할 행을 표시합니다. 아래 첫 검사에서 실패한 뒤 수정하고 다시 검사합니다. 실패 메시지를 숨기거나 기대값을 고쳐 통과시키지 않습니다.

```bash
uv run pytest tests/test_transfer.py --build-student -q
uv run pytest tests/test_build_lab.py --build-student -q
uv run python -m build_lab.runner complete --topic 로그인
```

새 계약과 기존 계약을 모두 검사합니다. 실제 연결에서는 `state.data.topic=계정`, P-02·IT지원팀, 최종 수용 판단을 확인합니다. 서버 응답의 topic을 별칭 그대로 두면 뒤의 Agent가 정규 업무명으로 다시 조회할 때 문제가 생길 수 있습니다. 어디서 이름을 정리하고 이후 단계에 무엇을 전달할지 설명합니다.

제출에는 변경 전 실패, 변경한 함수, 변경 후 검사, 실제 실행 기록, 직접 고른 미등록 별칭 반례 하나를 남깁니다. 함수 전체 풀이를 제공받았다면 그 범위를 표시합니다. 시간 안에 실제 연결이 끝나지 않으면 해당 실행은 미확인으로 남기고 풀이 뒤 이어서 확인합니다.

<details><summary>추가 비교: 다른 입력과 운영 문제</summary>

### 새 입력을 끝까지 추적합니다

여유가 있거나 수업 뒤 복습할 때 정산을 계정으로 바꾸어 달라져야 할 값 세 개와 그대로여야 할 제어 조건 하나를 적습니다. 다음 비교 전체를 독립 변경 15분에 추가로 수행하지 않습니다.

```bash
uv run python -m build_lab.runner complete --topic 계정
```

P-02·IT지원팀이 조회부터 검토까지 이어지는지 확인합니다. 예상과 다르면 처음 어긋난 단계의 필드를 기록합니다. 마지막 답변만 다시 고치기 전에 앞 단계에서 어떤 데이터가 전달됐는지 확인합니다.

다음에는 회신 대상을 비워 봅니다. 어느 단계에서 멈추고, 어떤 결과 필드가 더 이상 생성되지 않을지 먼저 예상합니다.

```bash
uv run python -m build_lab.runner complete --topic 계정 --contact ""
```

남은 시간에는 다음 중 하나를 골라 수정할 위치·예상 결과·확인 방법을 작성합니다.

1. 정책이 없는데 모델이 담당 팀을 단정합니다.
2. 수정 결과가 바뀌지 않는데 계속 호출합니다.
3. 원격 검토가 진행 중인데 완료 메시지를 보여줍니다.

먼저 다음 네 칸을 한 줄씩 적습니다. 선택한 문제의 입력은 무엇인지, 어떤 데이터를 사용할 수 있는지, 무엇을 성공으로 볼지, 실패를 어떤 결과로 확인할지 적습니다. “정확하게 답한다”처럼 해석이 다른 표현은 관찰 가능한 조건으로 바꿉니다.

입력·환경·채점 기준을 정하는 방법을 더 읽으려면 다음 글을 참고합니다.

[2026-08-25 원문](https://www.langchain.com/blog/building-agent-environments-and-tasks)

모델·도구·State·루프·프로토콜 중 어떤 부분을 고칠지 구체적인 함수나 값으로 적습니다. 코드가 아직 완성되지 않았어도 배운 개념으로 문제를 설명할 수 있습니다.

심화 반례: “P-01: 재무지원팀에는 절대 문의하지 마세요.”는 이름·ID 검사를 통과합니다. 로컬 `inspect_draft`와 원격 `verify`는 별도 함수이지만 모두 이름·ID를 중심으로 검사하므로 두 번 검사해도 같은 맹점이 남습니다. 검증 횟수와 검증 기준의 독립성이 어떻게 다른지 설명합니다.

</details>
</section>

<section class="slide" id="solution">

## 풀이와 다음 학습 · 10분

변경 전후를 기록한 뒤 `build_lab/transfer_solution.py`를 읽습니다. 풀이에서는 입력 공백을 정리하고 별칭을 정규 업무명으로 바꾼 다음 기존 정책을 조회합니다. 반환 topic도 정규 업무명으로 맞춥니다. 정책 복제 없이 조회 경계 한 곳에 규칙을 둔 이유를 자신의 선택과 비교합니다.

`"로그인" in topic`으로 구현하면 `로그인 장애`까지 계정으로 처리해 계약을 어깁니다. 별칭을 정책 dict에 추가하면 현재 요청을 처리하기 위해 공유 원본까지 바꾸게 됩니다. 별칭 입력만 통과해도 정상·미등록 입력이 달라졌다면 완료가 아닙니다.

검사 명령에서 `--build-student`를 빼면 별도 변경 풀이를 검사합니다. 풀이 통합 실행은 `uv run python -m build_lab.transfer_solution complete --topic 로그인`입니다. 학생 구현의 실행과 구분합니다.

회신 대상이 빈 요청은 `state.decision=ask`, `state.visited=["lookup", "ask"]`로 종료합니다. `state.draft`에는 추가 확인 문장이 있고 `state.history=[]`, `trace=[]`입니다. 모델 초안·수정·원격 검토를 실행하지 않으므로 `review` 필드는 없습니다. 오류 때문에 빠진 결과와 의도한 분기로 생성되지 않은 결과를 구분합니다.

계정 요청의 성공 기준은 P-02와 IT지원팀이 나온다는 사실뿐 아니라 같은 요청의 검토가 통과했는지도 포함합니다. 답변 문장이 기준 예시와 완전히 같을 필요는 없습니다.

첫 문제는 조회 결과와 답변의 일치 검사를, 두 번째는 개선 없음과 호출 예산을, 세 번째는 Task 상태와 artifact 검증을 확인합니다. 프롬프트 한 줄 변경만으로 모두 해결됐다고 단정하지 않습니다.

예시 풀이: 입력은 “등록되지 않은 출장 정책 문의”, 데이터는 제공 정책 두 건, 성공 조건은 “임의의 팀을 만들지 않고 추가 질문”, 실패 증거는 “도구가 found=false인데 최종 답변에 새 담당 팀을 단정”입니다. 정상 정산 문의도 함께 검사해야 기존 기능을 망가뜨리지 않았는지 알 수 있습니다.

자동 코드 검사는 계약을 지켰는지, 모델 평가는 다양한 질문에서도 적절히 행동하는지를 봅니다. 자동 코드 검사 통과만으로 다양한 자연어 질문에서의 판단 능력까지 입증하지는 않습니다.

[DeepAgents 평가 설계](https://www.langchain.com/blog/how-we-build-evals-for-deep-agents)

공통 도달점은 조회 도구·LangChain Agent 구성·업무 분기·MCP 등록·A2A 수용 조건의 다섯 구현과 실행 결과 해석입니다. 제공 Graph의 상태·노드·간선을 읽고, 제공 루프의 성공·정체·예산 종료를 설명합니다. 요청·버전 검사는 이 실습이 A2A 산출물에 추가한 업무 계약입니다. Loop·Graph Engineering의 실제 담론과 활용 활동에서는 반복 작업의 시작·선택·종료 조건과 역할별 의존성·산출물 계약을 정의합니다. 전체 그래프·루프 재구현, 영속 실행 복구·동시성·권한은 선택 심화입니다. 이 하루만으로 운영 수준의 Agent 시스템을 완성했다고 보지는 않습니다.

제출에는 자신의 구현, 정상·추가 확인 실행 기록, 이전 버전 결과에 대한 수용 함수의 보류 기록, 직접 만든 반례와 수정 이유, `HARNESS_WORKSHEET.md`를 포함합니다. 하네스 계정 없이 작성한 작업 명세와 시연·풀이 비교도 동일한 판단 기준으로 설명합니다. 제공받은 함수와 직접 구현한 부분을 구분합니다. 다른 사람보다 빨리 끝냈는지는 평가 기준이 아닙니다.

공통 활동을 돌아보려면 [Loop·Graph Engineering의 실제 담론과 활용](./engineering)을 읽습니다. 전체 그래프·수정 루프를 직접 작성하고 싶은 경우에는 [선택 심화](./build#loop)에서 기존 계약과 검사 조건을 유지하며 구현합니다.

</section>

<nav class="chapnav"><a href="../toc">전체 목차</a></nav>
</div></div>
